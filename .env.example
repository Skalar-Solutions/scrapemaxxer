# Scraping settings (copy to .env and adjust)
USER_AGENT=
REQUEST_TIMEOUT=30
REQUEST_DELAY=0
# http | dynamic | stealthy  (dynamic/stealthy need a browser; macOS 12 cannot install one)
FETCHER=http
OUTPUT_DIR=output
